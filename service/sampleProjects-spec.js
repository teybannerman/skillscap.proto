describe('sampleProjects', function() {

  beforeEach(module('skillscap-proto'));

  it('should ...', inject(function(sampleProjects) {

	//expect(sampleProjects.doSomething()).toEqual('something');

  }));

});